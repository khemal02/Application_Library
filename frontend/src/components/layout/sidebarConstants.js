// Shared between Sidebar.jsx and SidebarToggleHandle.jsx so the floating toggle handle always
// lines up exactly on the sidebar's right edge, in both the expanded and collapsed widths.
export const DRAWER_WIDTH = 260;
export const COLLAPSED_WIDTH = 76;
